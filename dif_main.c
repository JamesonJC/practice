#include "main.h"


int main(void)
{
	ssize_t n;
	size_t buf = 0;
	char *in;

	printf("Enter something: ");
	/*n = _getline(in, buf, 0);

	printf("Input is: %s and no. chars are: %ld", in, n);*/
	return (0);
}
